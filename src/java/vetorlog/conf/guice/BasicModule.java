package vetorlog.conf.guice;

import com.google.inject.AbstractModule;
import vetorlog.model.util.relational.DatabaseManager;

public class BasicModule extends AbstractModule {

    @Override
    protected void configure() {
    }
}